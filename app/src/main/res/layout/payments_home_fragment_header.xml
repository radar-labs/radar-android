<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:viewBindingIgnore="true"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/payments_home_fragment_header"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <!-- Balance: full-width MoneyView. The hide/show toggle lives in the toolbar (top-left
         nav icon in tab mode, leading menu item in standalone mode) so the balance can sit
         centered like iOS. -->
    <org.thoughtcrime.securesms.payments.MoneyView
        android:id="@+id/payments_home_fragment_header_balance"
        android:layout_width="0dp"
        android:layout_height="48sp"
        android:gravity="center"
        app:autoSizeMaxTextSize="48sp"
        app:autoSizeMinTextSize="14sp"
        app:autoSizeTextType="uniform"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:money="BTC:7500000000000" />

    <!-- Shown while the wallet balance is being fetched from the Breez SDK (the first fetch
         requires a network connect). Overlays the balance slot and is hidden once a balance
         arrives. -->
    <ProgressBar
        android:id="@+id/payments_home_fragment_header_balance_loading"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@+id/payments_home_fragment_header_balance"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/payments_home_fragment_header_balance"
        tools:visibility="visible" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/payments_home_fragment_header_exchange"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="16dp"
        android:paddingBottom="16dp"
        android:textAppearance="@style/Signal.Text.Body"
        android:textColor="@color/signal_icon_tint_primary"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/payments_home_fragment_header_balance"
        tools:text="$55.00" />

    <org.thoughtcrime.securesms.components.PaymentPillStrip
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/payments_home_fragment_header_exchange">

        <include layout="@layout/payment_pill_strip" />

    </org.thoughtcrime.securesms.components.PaymentPillStrip>

</androidx.constraintlayout.widget.ConstraintLayout>
